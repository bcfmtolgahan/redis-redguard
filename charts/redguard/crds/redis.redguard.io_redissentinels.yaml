---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: redissentinels.redis.redguard.io
spec:
  group: redis.redguard.io
  names:
    kind: RedisSentinel
    listKind: RedisSentinelList
    plural: redissentinels
    singular: redissentinel
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.masterNode
      name: Master
      type: string
    - jsonPath: .status.readyReplicas
      name: Replicas
      type: integer
    - jsonPath: .status.readySentinels
      name: Sentinels
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: RedisSentinel is the Schema for the redissentinels API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of RedisSentinel
            properties:
              redisConfig:
                description: RedisConfig defines Redis server configuration
                properties:
                  auth:
                    description: Auth defines authentication configuration
                    properties:
                      secretName:
                        description: |-
                          SecretName is the name of the secret containing Redis password
                          Key must be "password"
                        type: string
                    type: object
                  customConfig:
                    additionalProperties:
                      type: string
                    description: CustomConfig allows custom Redis configuration
                    type: object
                  image:
                    default: redis:7-alpine
                    description: Image is the Redis Docker image
                    type: string
                  replicas:
                    default: 3
                    description: Replicas is the number of Redis replicas
                    format: int32
                    minimum: 1
                    type: integer
                  resources:
                    description: Resources defines resource requirements for Redis
                      pods
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This field depends on the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                            request:
                              description: |-
                                Request is the name chosen for a request in the referenced claim.
                                If empty, everything from the claim is made available, otherwise
                                only the result of this request.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                  storage:
                    description: Storage defines persistent storage configuration
                    properties:
                      size:
                        anyOf:
                        - type: integer
                        - type: string
                        default: 1Gi
                        description: Size is the storage size
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      storageClassName:
                        description: StorageClassName is the storage class name
                        type: string
                    type: object
                type: object
              sentinelConfig:
                description: SentinelConfig defines Sentinel configuration
                properties:
                  customConfig:
                    additionalProperties:
                      type: string
                    description: CustomConfig allows custom Sentinel configuration
                    type: object
                  downAfterMilliseconds:
                    default: 5000
                    description: DownAfterMilliseconds is the time in ms before marking
                      instance as down
                    format: int32
                    type: integer
                  failoverTimeout:
                    default: 10000
                    description: FailoverTimeout is the failover timeout in milliseconds
                    format: int32
                    type: integer
                  parallelSyncs:
                    default: 1
                    description: ParallelSyncs is the number of replicas that can
                      be reconfigured in parallel
                    format: int32
                    type: integer
                  quorum:
                    default: 2
                    description: Quorum is the number of Sentinels that need to agree
                      about master failure
                    format: int32
                    minimum: 2
                    type: integer
                  replicas:
                    default: 3
                    description: Replicas is the number of Sentinel instances (min
                      3 recommended)
                    format: int32
                    minimum: 3
                    type: integer
                  resources:
                    description: Resources defines resource requirements for Sentinel
                      pods
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This field depends on the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                            request:
                              description: |-
                                Request is the name chosen for a request in the referenced claim.
                                If empty, everything from the claim is made available, otherwise
                                only the result of this request.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                type: object
              serviceType:
                default: ClusterIP
                description: ServiceType specifies the type of service for external
                  access
                enum:
                - ClusterIP
                - NodePort
                - LoadBalancer
                type: string
              tls:
                description: TLS defines TLS/SSL configuration
                properties:
                  caSecretRef:
                    description: |-
                      CASecretRef references a Secret containing CA certificate
                      Secret must have "ca.crt" key
                    type: string
                  certificateSecretRef:
                    description: |-
                      CertificateSecretRef references a Secret containing TLS certificates
                      Secret must have "tls.crt" and "tls.key" keys
                    type: string
                  enabled:
                    description: Enabled indicates if TLS is enabled
                    type: boolean
                  mutualTLS:
                    description: MutualTLS enables mutual TLS authentication
                    type: boolean
                type: object
            required:
            - redisConfig
            - sentinelConfig
            type: object
          status:
            description: status defines the observed state of RedisSentinel
            properties:
              conditions:
                description: |-
                  Conditions represent the current state of the RedisSentinel resource.
                  Standard condition types:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastFailoverTime:
                description: LastFailoverTime is the timestamp of the last failover
                format: date-time
                type: string
              masterNode:
                description: MasterNode is the current Redis master pod name
                type: string
              phase:
                description: Phase represents the current phase of the RedisSentinel
                type: string
              readyReplicas:
                description: ReadyReplicas is the number of ready Redis replicas
                format: int32
                type: integer
              readySentinels:
                description: ReadySentinels is the number of ready Sentinel instances
                format: int32
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
